{
  "name": "api-jsonplaceholder-typescript",
  "version": "1.0.0",
  "description": "Consumo de API REST con JSONPlaceholder usando TypeScript",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "clean": "rm -rf dist",
    "start": "npx serve -l 8080",
    "dev": "npm run build && npm start"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "serve": "^14.2.0"
  }
}